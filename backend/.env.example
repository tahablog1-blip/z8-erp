# ── قاعدة البيانات: نفس قاعدة النظام الحالي بالحرف ──
DATABASE_URL=postgresql://postgres:123456@localhost:5432/z8_car_manager
# داخل Docker على قاعدة الجهاز: postgresql://postgres:123456@host.docker.internal:5432/z8_car_manager

# ── JWT: نفس مفتاح النظام الحالي → التوكنات متبادلة بين النظامين ──
JWT_SECRET=z8SecretKey2026XyzAbc123456789QwErTy
JWT_EXPIRES_DAYS=7

PORT=4001
CORS_ORIGINS=http://localhost:3000
